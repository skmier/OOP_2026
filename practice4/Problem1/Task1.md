# 1. What is the core difference between a class and an interface?
## ``` 1: Class can implement many interfaces.``` ``` Class can extend only one abstract class```

## ``` 2: Interface has only constant(static final) variables . Abstract class can have instance variables ```

## ```3:Interface has abstract methods . Abstract can have concrete methods ```
***
# 2. Can interfaces have fields? If yes, what are the implicit modifiers for them?

## ``` A: Yes interfaces can have fields. Interfaces have public static and final modifiers ```
***

# 3 Can a class implement multiple interfaces? Explain why this is useful
## ``` Yes class can implement multiple interfaces. Why is this useful? A class can have different capabilities. Intarfaces allow avoid conflicts Different classes can implement the same interface in different ways.```
